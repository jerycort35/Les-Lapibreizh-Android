from pathlib import Path
P=Path('app/src/main/java/fr/leslapibreizh/mediatheque/MainActivity.kt')
s=P.read_text(encoding='utf-8')
start=s.index('    /** V0.6.8: catalogue réellement dynamique et défilant. */')
end=s.index('    private fun refreshCategoryActions()',start)
new='    /** V0.6.31 : page Catégories reconstruite sur la maquette maître. */\n    private fun showCategoryCatalog(video: Boolean) {\n        generation++\n        screenMode = if (video) "videos-catalog" else "images-catalog"\n        route = if (video) Route.VIDEOS else Route.IMAGES\n        videoCategoryTab = video\n        selected.clear()\n        if (video) { showCategoryManager(true, CategoryEntry.VIDEOS); return }\n\n        var categories=savedCategories().toMutableList()\n        if (categories.size < 8 || categories.take(8).any { !it.startsWith("Catégorie ") }) {\n            categories=(1..8).map { "Catégorie $it" }.toMutableList()\n            prefs.edit().putString("category_names",categories.joinToString("\\u001f")).apply()\n        }\n\n        val page=LinearLayout(this).apply {\n            orientation=LinearLayout.VERTICAL\n            setBackgroundColor(black)\n            setPadding(dp(6),0,dp(6),0)\n        }\n        page.addView(galleryHeader(false) { showHome() },LinearLayout.LayoutParams(-1,dp(205)))\n\n        val identity=LinearLayout(this).apply {\n            orientation=LinearLayout.HORIZONTAL; gravity=Gravity.CENTER_VERTICAL\n            setPadding(dp(4),dp(3),dp(4),dp(3))\n        }\n        identity.addView(TextView(this).apply {\n            text="?"; textSize=44f; gravity=Gravity.CENTER; setTextColor(cream)\n            background=GradientDrawable().apply { setColor(Color.rgb(12,12,12)); setStroke(dp(1),gold) }\n        },LinearLayout.LayoutParams(dp(68),dp(68)).apply { rightMargin=dp(8) })\n        val labels=LinearLayout(this).apply { orientation=LinearLayout.VERTICAL }\n        labels.addView(simpleLabel("Catégorie 1",20f,cream))\n        labels.addView(simpleLabel("0 image",11f,cream))\n        labels.addView(simpleLabel("Apprendre  •  Comprendre  •  Protéger  •  Partager",10f,gold))\n        identity.addView(labels,LinearLayout.LayoutParams(0,-2,1f))\n        identity.addView(action("▰  Changer\\nde catégorie") { showImagesCategories() },LinearLayout.LayoutParams(dp(145),dp(62)))\n        page.addView(identity)\n\n        val searchRow=LinearLayout(this).apply { orientation=LinearLayout.HORIZONTAL }\n        searchRow.addView(action("⌕  Rechercher une image…") { openSearch(Route.IMAGES) },\n            LinearLayout.LayoutParams(0,dp(52),1f).apply { rightMargin=dp(4) })\n        searchRow.addView(action("☷  Filtres") { openFilters() },LinearLayout.LayoutParams(dp(105),dp(52)))\n        page.addView(searchRow)\n        page.addView(simpleLabel("Taille des miniatures",11f,cream).apply { gravity=Gravity.END })\n        val sizes=LinearLayout(this).apply { orientation=LinearLayout.HORIZONTAL }\n        listOf("▦\\nPetites","▦\\nMoyennes","▦\\nGrandes").forEach {\n            sizes.addView(action(it) {},LinearLayout.LayoutParams(0,dp(48),1f))\n        }\n        page.addView(sizes)\n\n        val sort=LinearLayout(this).apply { orientation=LinearLayout.HORIZONTAL; gravity=Gravity.CENTER_VERTICAL }\n        sort.addView(simpleLabel("Trier par :",13f,cream).apply { gravity=Gravity.CENTER },LinearLayout.LayoutParams(dp(82),dp(44)))\n        listOf("Date ↓","Nom","Type","Taille").forEach { t ->\n            sort.addView(action(t) {},LinearLayout.LayoutParams(0,dp(44),1f))\n        }\n        sort.addView(simpleLabel("▧   0 image",11f,cream).apply { gravity=Gravity.CENTER },LinearLayout.LayoutParams(dp(105),dp(44)))\n        page.addView(sort)\n\n        val grid=GridLayout(this).apply { columnCount=3; alignmentMode=GridLayout.ALIGN_BOUNDS; useDefaultMargins=false }\n        fun params()=GridLayout.LayoutParams().apply {\n            width=0; height=dp(128); columnSpec=GridLayout.spec(GridLayout.UNDEFINED,1f)\n            setMargins(dp(2),dp(2),dp(2),dp(2))\n        }\n        fun card(title:String, plus:Boolean=false)=LinearLayout(this).apply {\n            orientation=LinearLayout.VERTICAL; gravity=Gravity.CENTER\n            background=GradientDrawable().apply { setColor(Color.rgb(10,10,10)); setStroke(dp(1),gold) }\n            addView(simpleLabel(if(plus) "+" else "?",if(plus)50f else 44f,if(plus)gold else cream).apply { gravity=Gravity.CENTER },\n                LinearLayout.LayoutParams(-1,dp(72)))\n            addView(simpleLabel(title,13f,cream).apply { gravity=Gravity.CENTER },LinearLayout.LayoutParams(-1,dp(30)))\n            if(!plus) addView(simpleLabel("0 image",10f,cream).apply { gravity=Gravity.CENTER },LinearLayout.LayoutParams(-1,dp(20)))\n            setOnClickListener { if(plus) addCategoryDialog() else openGalleryCategory(title,Route.IMAGES) }\n        }\n        grid.addView(card("Nouvelle\\ncatégorie",true),params())\n        categories.take(8).forEach { grid.addView(card(it),params()) }\n        val categoryScroll=ScrollView(this).apply { setBackgroundColor(black); addView(grid,FrameLayout.LayoutParams(-1,-2)) }\n        page.addView(categoryScroll,LinearLayout.LayoutParams(-1,0,1f))\n\n        val actions=LinearLayout(this).apply { orientation=LinearLayout.VERTICAL }\n        val row1=LinearLayout(this).apply { orientation=LinearLayout.HORIZONTAL }\n        listOf("ⓧ  0 image\\nsélectionnée","Partager\\nChatGPT","Partager\\nInstagram","Partager\\nFacebook","Plus de\\npartages").forEach {\n            row1.addView(action(it) {},LinearLayout.LayoutParams(0,dp(54),1f))\n        }\n        actions.addView(row1)\n        val row2=LinearLayout(this).apply { orientation=LinearLayout.HORIZONTAL }\n        row2.addView(action("▰  Déplacer\\n(0 / 1000)") {},LinearLayout.LayoutParams(0,dp(54),1f))\n        row2.addView(action("▧  Envoyer à la galerie\\n(0 / 1000)") {},LinearLayout.LayoutParams(0,dp(54),1.3f))\n        row2.addView(dangerAction("Supprimer\\n(0 / 100 max)") {},LinearLayout.LayoutParams(0,dp(54),1f))\n        row2.addView(action("☑  Tout sélectionner") {},LinearLayout.LayoutParams(0,dp(54),1.15f))\n        actions.addView(row2)\n        page.addView(actions)\n        page.addView(navBar(Route.IMAGES),LinearLayout.LayoutParams(-1,dp(78)))\n        setContentView(page)\n    }\n\n'
s=s[:start]+new+s[end:]
s=s.replace('hint = "⌕  Rechercher une image ou vidéo…"', 'hint = if (route == Route.IMAGES && categoryFilter != null) "⌕  Rechercher une image…" else "⌕  Rechercher une image ou vidéo…"', 1)
old='''            labels.addView(simpleLabel("${allKnownMediaKeysForCategory(cat)} image(s)",12f,gold))
            identity.addView(labels,LinearLayout.LayoutParams(0,-2,1f))
'''
new2='''            labels.addView(simpleLabel("${allKnownMediaKeysForCategory(cat)} image(s)",12f,cream))
            labels.addView(simpleLabel("Apprendre  •  Comprendre  •  Protéger  •  Partager",11f,gold))
            identity.addView(labels,LinearLayout.LayoutParams(0,-2,1f))
'''
if old in s: s=s.replace(old,new2,1)
P.write_text(s,encoding='utf-8')
print('V0.6.31 appliquee')
